{
  "name": "pdf-skills",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p pdf2png && esbuild pdf2png.mjs --bundle --platform=node --outfile=pdf2png/run.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@hyzyla/pdfium": "^2.1.12",
    "pdfkit": "^0.18.0",
    "esbuild": "^0.21.4"
  }
}
